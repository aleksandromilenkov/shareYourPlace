const axios = require("axios");
function getCoordsFromAddress(address) {
  // dummy data because Google API requires Credit Card
  return {
    lat: 41.04242,
    lng: 23.03232,
  };
}

module.exports = getCoordsFromAddress;
